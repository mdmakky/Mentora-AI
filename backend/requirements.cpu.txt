-r requirements.txt

# CPU-only PyTorch index for low-memory, no-GPU servers
--extra-index-url https://download.pytorch.org/whl/cpu

# CPU-only stack for local semantic fallback (Gemini -> MiniLM -> hash)
torch==2.11.0+cpu ; platform_system == "Linux"
sentence-transformers==3.0.1
